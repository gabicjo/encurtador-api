# AGENTS.md

## Project Overview
Flask API for URL shortening ("Encurtador de links").

## Running the App
```bash
pip install -r requirements.txt
python -m flask run
```
Or run the main module directly if it exists (e.g., `python app.py`).

## Dependencies
- flask
- flask-cors